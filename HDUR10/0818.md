f_{0,0,n+1}=1
i: 
    f{i,num,min}
        <- f{i-1,num-1,x}
        <- f{i-1,num-1,min}*(n-min+1-num)
        <- f{i-1,num,min}*num

    f_{i,num,min}
        -> f{i+1,num+1,x}(suffix)
        -> f{i+1,num+1,min} (k=(n-num))
        ->...
ans=\sum_{min<=num}f{n,num,min}
O(n)

f{0,0}=1
i:
    f{i,num}
        -> f{i,num+1}
        -> f{i,num} (k=num)

ans = \sum_k f{n,k} * \sum_min {n-num\choose k-1}

spj: seq_1==n
ans = \min op2 + \sum_{seq_i>=2*op2}{ceil(seq_i - 2 * op2) / (2 * op2 + 1)}
    = \min op2 + \sum_{seq_i}{ max(0, ceil(seq_i + 1) / (2op2+1) - 1) }

## 1012
### debug
f[1][1][0][0][1]
    -> f[2][1][0][0][1]
###  WAed
填补的up是有序的，用A而非C，以及结算时用乘法而非直接上
